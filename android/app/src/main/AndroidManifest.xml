<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for every API call and the realtime Socket.IO connection
         this app makes — without it the app cannot reach the backend at
         all on a real device (only some emulator configs tolerate its absence). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Used by connectivity_plus to detect online/offline state. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Required on Android 13+ (API 33+) to actually show FCM push
         notifications; without it, notification permission requests are
         silently ignored on newer devices. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:label="katiya_station_rms"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <!-- The backend URL (ApiConstants.baseUrl) defaults to plain HTTP
             and is only known at build time via --dart-define, so it can't
             be pinned to a specific domain here. Android blocks plaintext
             HTTP by default since API 28 (Android 9) — without this flag
             every API call and the Socket.IO connection would silently
             fail to connect on any real device or modern emulator. Once
             the backend is served over HTTPS, this can be removed. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
